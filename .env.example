# Copy to .env.local and fill in your OWN keys. .env.local is gitignored.
# The backend builds a rotating pool from whichever of these are set (in this order),
# failing over to the next when one is rate-limited. All are read server-side only.

# Groq — free, no card, free vision (llama-4-scout). https://console.groq.com
GROQ_API_KEY=

# Google Gemini — free vision (flash-lite). Make the key in AI Studio, not Cloud Console.
GEMINI_API_KEY=

# OpenRouter — one key, many free models. https://openrouter.ai/keys
OPENROUTER_API_KEY=

# --- Cloud sync (M6, server-only — NEVER prefix these with VITE_) ---
# Neon pooled connection string (Vercel Marketplace → Neon injects this)
DATABASE_URL=
# Clerk backend secret (Vercel Marketplace → Clerk injects this)
CLERK_SECRET_KEY=
# Comma-separated allowed origins for Clerk token azp check (prod + preview URLs)
APP_ORIGINS=
